Photos referenced by `data/lost-found/*.json` files' `"photo"` field
live here, uploaded by hand via GitHub's own "Add file → Upload files"
button when approving a Lost & Found post -- see
`data/lost-found/README.md` for the full explanation. This file exists
just so the otherwise-empty folder is tracked by git.
