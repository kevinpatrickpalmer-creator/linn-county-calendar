name: Update Community Support

on:
  push:
    branches: [main]
    paths:
      - "data/support/**"
  workflow_dispatch: {}
  # Posts expire by age (see load_posts() in build_support_directory.py),
  # not just on a new submission -- this reruns the build daily so a
  # post actually drops off the site once it's stale, even during a
  # lull with nothing new coming in.
  schedule:
    - cron: "25 13 * * *"

permissions:
  contents: write

concurrency:
  group: update-support
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build docs/support.json
        run: python build_support_directory.py

      - name: Commit updated support file
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/support.json
          git diff --cached --quiet || git commit -m "Update Community Support"
          git push
